{
  "name": "crypto-dca-planner",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo root. Single Railway service builds/runs both the API and the Next.js UI (see Dockerfile).",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "dev:api": "cd backend && npm run dev",
    "dev:web": "cd frontend && npm run dev"
  }
}
